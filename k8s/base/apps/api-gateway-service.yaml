apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway-service
  namespace: comandalivre
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-gateway-service
  template:
    metadata:
      labels:
        app: api-gateway-service
    spec:
      containers:
      - name: api-gateway-service
        image: comandalivre/api-gateway-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: app-config
        - secretRef:
            name: app-secrets
        resources:
          requests:
            cpu: "100m"
            memory: "512Mi"
          limits:
            cpu: "1000m"
            memory: "1Gi"
---
apiVersion: v1
kind: Service
metadata:
  name: api-gateway-service
  namespace: comandalivre
spec:
  selector:
    app: api-gateway-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080