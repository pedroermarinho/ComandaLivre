apiVersion: apps/v1
kind: Deployment
metadata:
  name: comandalivre-service
  namespace: comandalivre
spec:
  replicas: 1
  selector:
    matchLabels:
      app: comandalivre-service
  template:
    metadata:
      labels:
        app: comandalivre-service
    spec:
      containers:
      - name: comandalivre-service
        image: comandalivre/comandalivre-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: app-config
        - secretRef:
            name: app-secrets
        resources:
          requests:
            cpu: "100m"
            memory: "512Mi"
          limits:
            cpu: "1000m"
            memory: "1Gi"
---
apiVersion: v1
kind: Service
metadata:
  name: comandalivre-service
  namespace: comandalivre
spec:
  selector:
    app: comandalivre-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080