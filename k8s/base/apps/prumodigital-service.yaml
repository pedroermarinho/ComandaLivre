apiVersion: apps/v1
kind: Deployment
metadata:
  name: prumodigital-service
  namespace: comandalivre
spec:
  replicas: 1
  selector:
    matchLabels:
      app: prumodigital-service
  template:
    metadata:
      labels:
        app: prumodigital-service
    spec:
      containers:
      - name: prumodigital-service
        image: comandalivre/prumodigital-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: app-config
        - secretRef:
            name: app-secrets
        resources:
          requests:
            cpu: "100m"
            memory: "512Mi"
          limits:
            cpu: "1000m"
            memory: "1Gi"
---
apiVersion: v1
kind: Service
metadata:
  name: prumodigital-service
  namespace: comandalivre
spec:
  selector:
    app: prumodigital-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080